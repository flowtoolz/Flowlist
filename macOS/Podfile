use_frameworks!

platform :osx, '10.11'

target 'Flowlist' do

    pod 'GetLaid', :path=> '../../Github Repos/Frameworks/GetLaid'
    pod 'UIObserver', :path=> '../../Github Repos/Frameworks/UIObserver'
    pod 'UIToolz', :path=> '../../Github Repos/Frameworks/UIToolz'
    pod 'FoundationToolz', :path=> '../../Github Repos/Frameworks/FoundationToolz'
    pod 'SwiftObserver', :path=> '../../Github Repos/Frameworks/SwiftObserver'
    pod 'SwiftyToolz', :path=> '../../Github Repos/Frameworks/SwiftyToolz'

end

post_install do |installer|
    installer.pods_project.targets.each do |target|
        target.build_configurations.each do |config|
            config.build_settings['MACOSX_DEPLOYMENT_TARGET'] = '10.11'
        end
    end
end
